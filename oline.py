from datetime import datetime

from fastapi import FastAPI
from bwroline import user
from bwroline.bmu import Bmu, B, Shd, Ghl, Egb, Btg


def PROS(self):
    print('==========PROS-O-line Multi-links Communication Ltd Company==========')
    print(f'\t Id No. \t Name \t\t Position \t\t Owner Monthly Salary \t\t Website Name')


def plus(self):
    click = 0
    if click <= 987654321:
        click += 1


class PROSOline:
    def __init__(self):
        ''' '''
        self.d = [{'name': 'Dakoyi'}, {'owner website': Bmu.shw()}]
        self.f = [{'DI': 'Fredrick Dakoyi'}, {'owner website': sh}]
        self.n = 'NA'
        self.y = 'YI'
        self.fc = 'FC'
        self.acct_no = str
        self.acct_bal = float
        self.e = self.employee()

    class employee:
        def __init__(self):
            self.pay_per_use: user.get_current_active_user()+{datetime.now()}
            self.company = PROSOline
            self.curr_bal = 500.00
            self.dep = f'amount: {self.curr_bal}, {PROSOline.plus(1)} user: {user.get_current_active_user()}'
            self.details = [{'d': 'Dakoyi', 'Account': '0025531607', 'Account_bal': float}]
            if self.curr_bal <= 600.00:
                self.curr_bal += 4.556655e6

        def employee(id: int, name: str, post: str, m_s: float, web: str):
            print(f'\t {id}  \t\t {name} \t {post} \t\t {m_s} \t\t\t\t\t\t {web}')


b = PROSOline()
print(b.employee.employee(1, 'Dakoyi', 'Manager', 20000, 'bwroline.com'))
print(b.employee.employee(2, 'Fredrick', 'Inspector', 10000, 'shamudoents.com'))
print(b.employee.employee(3, 'Ndakoyi', 'Info. man.', 15000, 'greathusseinlegalpractitioners.com'))
print(b.employee.employee(4, 'Joyce', '\t Info-tech', 12000, 'ecwagoodnewsbwr.com'))
print(b.employee.employee(5, 'Fidelia', 'Info-g dpt', 10000, 'btgees.com'))
